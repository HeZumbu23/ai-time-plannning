name: ai_time_planning
description: Persönliches TODO- und Coaching-System (Web + Android) mit Supabase Backend.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  supabase: ^2.8.0
  go_router: ^14.0.0
  http: ^1.2.0
  shared_preferences: ^2.2.3
  mobile_scanner: ^5.0.0
  webview_flutter: ^4.2.0

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/html/mindmap.html
